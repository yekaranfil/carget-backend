server:
  port: 8084

spring.jpa:
  database: POSTGRESQL
  hibernate.ddl-auto: update
  show-sql: true

spring.datasource:
  driverClassName: org.postgresql.Driver
  url: jdbc:postgresql://localhost:5432/carget
  username: pst
  password: 123


spring:
  sql:
    init:
      platform: postgres
  mvc:
    view:
      prefix: webapp/view/
      suffix: .jsp
    pathmatch:
      matching-strategy: ant_path_matcher
